# Core — required for app.py (main dashboard)
flask>=3.0
ollama>=0.6
pystray>=0.19
Pillow>=10.0

# Voice Bot — required for voice_bot.py (optional module)
# flask-socketio>=5.0
# sherpa-onnx>=1.10
# kokoro>=0.9
# soundfile>=0.12
# numpy>=1.26
# unidic-lite          # Windows MeCab config for Japanese TTS
